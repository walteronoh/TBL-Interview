// Assuming we have imported jest
import { isPerfectSquare, isFibonacci } from './Q2';

describe("Running test for Perfect Square", () => {
    it("Should return true if it is a perfect square", () => {
        isPerfectSquare(4);
    });

    it("Should return false if it is not a perfect square", () => {
        isPerfectSquare(9);
    });
});

describe("Running test for Fibonacci", () => {
    it("Should be a Fibonacci number", () => {
        isPerfectSquare(4);
    });

    it("Should not be a Fibonacci number", () => {

    });
});